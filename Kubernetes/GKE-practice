

# command to create cluster
gcloud container clusters create gke-practice-cluster \
  --zone=asia-south1-c \
  --num-nodes=2 \
  --machine-type=e2-medium

# We need service account for creating cluster on bastion server
gcloud auth list
for now added my account using
gcloud auth login --no-launch-browser
# on console 
While on the bastion instance details page (when stopped), click Edit.
Scroll to Identity and API access (or a similarly named section).
Service account: choose bastion-gke-sa (the service account you created).
Access scopes: select Allow full access to all Cloud APIs


# prerequisites
# Update system
sudo apt-get update
sudo apt-get install -y apt-transport-https ca-certificates gnupg curl
# Add Google Cloud SDK repository
echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] https://packages.cloud.google.com/apt cloud-sdk main" | sudo tee -a /etc/apt/sources.list.d/google-cloud-sdk.list
# Import Google Cloud public key
curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key --keyring /usr/share/keyrings/cloud.google.gpg add -
# Update and install
sudo apt-get update
sudo apt-get install -y google-cloud-sdk
# Install additional components
sudo apt-get install -y google-cloud-sdk-gke-gcloud-auth-plugin
sudo apt-get install -y kubectl

# helm 
# download + run the installer (official script)
curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
helm version

# cluster create
gcloud container clusters create my-gke-cluster \
  --zone=asia-south1-c \
  --num-nodes=2 \
  --machine-type=e2-standard-4

# ingress controller install

helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm repo update
helm install ingress-nginx ingress-nginx/ingress-nginx \
  --namespace ingress-nginx --create-namespace \
  --set controller.replicaCount=2

# kubenetes manifest files and 
kubectl apply -f deployment.yaml

# to delete cluster 
gcloud container clusters delete gke-practice-cluster \
  --zone=asia-south1-c \
  --project=artemis-399212 \
  --quiet


